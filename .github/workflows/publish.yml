name: 📚 نشر اقتباس يومي

on:
  schedule:
    - cron: "0 17 * * *"  # كل يوم الساعة 8 مساءً بتوقيت السعودية
  workflow_dispatch:      # عشان تقدر تشغله يدوي

jobs:
  send-quote:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 جلب الكود
        uses: actions/checkout@v4

      - name: 🐍 تثبيت بايثون
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: 📦 تثبيت المكتبات
        run: pip install -r requirements.txt

      - name: 🤖 تشغيل البوت
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        run: python bot.py
